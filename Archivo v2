local AutoExecuteCode = [[
-- BEN HUB V25 - FIXED EXECUTION & FULL OPTIONS
local Players = game:GetService("Players")
local VirtualInputManager = game:GetService("VirtualInputManager")
local RunService = game:GetService("RunService")
local HttpService = game:GetService("HttpService")

local LocalPlayer = Players.LocalPlayer
local CoreGui = game:FindFirstChild("CoreGui") or LocalPlayer:WaitForChild("PlayerGui")

local BossList = {"black cell", "future boss", "doctor boss", "galaxy boss", "granola", "moro", "black frieza", "black buu"}
local ZeniBossList = {"zeno", "zeno zama", "zenozama", "king zeno"}
local LootList = {"chest", "cofre", "money", "zeni", "cash", "coins", "loot", "item", "box"}

local FixedDragonBallCoords = {
    Vector3.new(120, 15, -450),
    Vector3.new(-850, 45, 1200),
    Vector3.new(450, 20, 850),
    Vector3.new(-300, 10, -900),
    Vector3.new(900, 35, -200),
    Vector3.new(-600, 50, 500),
    Vector3.new(0, 25, 0)
}

local Config = {
    Delay = 0.3,
    SkillDelay = 1.5,
    ZenkaiAmount = 10,
    Skills = {Enum.KeyCode.One, Enum.KeyCode.Two, Enum.KeyCode.Three, Enum.KeyCode.Four, Enum.KeyCode.Five, Enum.KeyCode.Six, Enum.KeyCode.Seven, Enum.KeyCode.Eight},
    CustomSkills = {Enum.KeyCode.Z, Enum.KeyCode.X, Enum.KeyCode.C, Enum.KeyCode.V, Enum.KeyCode.G}
}

local Flags = {
    AutoFarmBoss = false,
    AutoZeni = false,
    AutoLockBoss = false,
    AutoClick = false,
    AutoSkills = false,
    AutoCustomSkills = false,
    AutoRetry = true,
    AutoZenkai = false,
    AutoChest = true,
    AutoDragonBalls = false
}

-- SISTEMA DE GUARDADO (JSON)
local FolderName = "BenHubConfig"
local FileName = FolderName .. "/settings.json"

pcall(function()
    if isfolder and not isfolder(FolderName) then makefolder(FolderName) end
end)

local function SaveSettings()
    pcall(function()
        if writefile then
            local dataToSave = { SavedFlags = Flags, SavedSkillDelay = Config.SkillDelay, SavedZenkaiAmount = Config.ZenkaiAmount }
            writefile(FileName, HttpService:JSONEncode(dataToSave))
        end
    end)
end

local function LoadSettings()
    pcall(function()
        if isfile and isfile(FileName) and readfile then
            local result = HttpService:JSONDecode(readfile(FileName))
            if result and result.SavedFlags then
                for k, v in pairs(result.SavedFlags) do Flags[k] = v end
                if result.SavedSkillDelay then Config.SkillDelay = result.SavedSkillDelay end
                if result.SavedZenkaiAmount then Config.ZenkaiAmount = result.SavedZenkaiAmount end
            end
        end
    end)
end
LoadSettings()
local ZenkaiCount = 0

-- FUNCIONES DE ACCIÓN
local function pressKey(keyCode)
    pcall(function()
        VirtualInputManager:SendKeyEvent(true, keyCode, false, game)
        task.wait(0.1)
        VirtualInputManager:SendKeyEvent(false, keyCode, false, game)
    end)
end

local function clickMouse()
    pcall(function()
        VirtualInputManager:SendMouseButtonEvent(0, 0, 0, true, game, 1)
        task.wait(0.05)
        VirtualInputManager:SendMouseButtonEvent(0, 0, 0, false, game, 1)
    end)
end

local function forceClick(obj)
    pcall(function()
        if obj and obj.AbsolutePosition and obj.AbsoluteSize then
            local pos = obj.AbsolutePosition + (obj.AbsoluteSize / 2)
            VirtualInputManager:SendMouseButtonEvent(pos.X, pos.Y + 36, 0, true, game, 1)
            task.wait(0.05)
            VirtualInputManager:SendMouseButtonEvent(pos.X, pos.Y + 36, 0, false, game, 1)
        end
    end)
end

-- BUCLE DE HABILIDADES
task.spawn(function()
    while true do
        task.wait(Config.SkillDelay)
        if Flags.AutoFarmBoss or Flags.AutoZeni then
            if Flags.AutoSkills then
                for _, key in ipairs(Config.Skills) do
                    if not (Flags.AutoFarmBoss or Flags.AutoZeni) then break end
                    pressKey(key); task.wait(0.1)
                end
            end
            if Flags.AutoCustomSkills then
                for _, key in ipairs(Config.CustomSkills) do
                    if not (Flags.AutoFarmBoss or Flags.AutoZeni) then break end
                    pressKey(key); task.wait(0.1)
                end
            end
        end
    end
end)

-- BUCLE GENERAL
task.spawn(function()
    local isTransformed = false
    while true do
        task.wait(Config.Delay)
        local char = LocalPlayer.Character
        local hrp = char and char:FindFirstChild("HumanoidRootPart")

        -- Auto Retry
        pcall(function()
            if Flags.AutoRetry then
                for _, parent in ipairs({LocalPlayer.PlayerGui, CoreGui}) do
                    for _, obj in ipairs(parent:GetDescendants()) do
                        if (obj:IsA("TextButton") or obj:IsA("ImageButton")) and obj.Visible then
                            local text = (obj:IsA("TextButton") and obj.Text or ""):lower()
                            if text:find("retry") or text:find("replay") or text:find("rematch") or text:find("luchar") or text:find("again") or text:find("ready") then
                                forceClick(obj); task.wait(0.5)
                            end
                        end
                    end
                end
            end
        end)

        if hrp then
            -- Auto Chests
            pcall(function()
                if Flags.AutoChest then
                    for _, obj in ipairs(workspace:GetDescendants()) do
                        if obj:IsA("BasePart") or obj:IsA("Model") then
                            local name = obj.Name:lower()
                            for _, lootName in ipairs(LootList) do
                                if name:find(lootName) then
                                    local part = obj:IsA("BasePart") and obj or obj:FindFirstChildWhichIsA("BasePart", true)
                                    if part and (hrp.Position - part.Position).Magnitude < 40 then
                                        hrp.CFrame = part.CFrame; pressKey(Enum.KeyCode.F)
                                    end
                                end
                            end
                        end
                    end
                end
            end)

            -- Auto Zenkai
            pcall(function()
                if Flags.AutoZenkai and ZenkaiCount < Config.ZenkaiAmount then
                    for _, parent in ipairs({LocalPlayer.PlayerGui, CoreGui}) do
                        for _, obj in ipairs(parent:GetDescendants()) do
                            if (obj:IsA("TextButton") or obj:IsA("ImageButton")) and obj.Visible then
                                local text = (obj:IsA("TextButton") and obj.Text or ""):lower()
                                if text:find("zenkai") or text:find("rebirth") or text:find("renacer") then
                                    forceClick(obj); ZenkaiCount = ZenkaiCount + 1; task.wait(1)
                                    if ZenkaiCount >= Config.ZenkaiAmount then Flags.AutoZenkai = false end
                                    break
                                end
                            end
                        end
                    end
                end
            end)

            -- Auto Dragon Balls
            pcall(function()
                if Flags.AutoDragonBalls then
                    local targetPart = nil
                    for _, v in ipairs(workspace:GetDescendants()) do
                        local n = v.Name:lower()
                        if n:find("star") or n:find("dragon") or n:find("esfera") or n:find("ball") then
                            targetPart = v:IsA("BasePart") and v or v:FindFirstChildWhichIsA("BasePart", true)
                            if targetPart then break end
                        end
                    end
                    if targetPart then
                        hrp.CFrame = targetPart.CFrame + Vector3.new(0, 1.5, 0)
                        pressKey(Enum.KeyCode.F)
                    end
                end
            end)

            -- Auto Farm / Zeni
            pcall(function()
                local list = Flags.AutoZeni and ZeniBossList or (Flags.AutoFarmBoss and BossList or nil)
                if list then
                    local boss = nil
                    for _, v in ipairs(workspace:GetDescendants()) do
                        if v:IsA("Model") then
                            local name = v.Name:lower()
                            for _, t in ipairs(list) do
                                if name:find(t) then
                                    local hum = v:FindFirstChildOfClass("Humanoid")
                                    if hum and hum.Health > 0 then
                                        boss = v:FindFirstChild("HumanoidRootPart") or v:FindFirstChild("Torso") or v:FindFirstChild("UpperTorso")
                                        break
                                    end
                                end
                            end
                        end
                        if boss then break end
                    end

                    if boss then
                        if not isTransformed then pressKey(Enum.KeyCode.B); task.wait(1); isTransformed = true end
                        hrp.CFrame = boss.CFrame * CFrame.new(0, 0, 3)
                        if Flags.AutoLockBoss then pressKey(Enum.KeyCode.F) end
                        if Flags.AutoClick then pressKey(Enum.KeyCode.E); clickMouse() end
                    else
                        isTransformed = false
                    end
                end
            end)
        end
    end
end)

-- INTERFAZ GRÁFICA (GARANTIZADA)
local ScreenGui = Instance.new("ScreenGui")
ScreenGui.Name = "BenHubV25"
pcall(function()
    ScreenGui.Parent = CoreGui
end)
if not ScreenGui.Parent then
    ScreenGui.Parent = LocalPlayer:WaitForChild("PlayerGui")
end

local MainFrame = Instance.new("Frame", ScreenGui)
MainFrame.Size = UDim2.new(0, 620, 0, 410)
MainFrame.Position = UDim2.new(0.5, -310, 0.5, -205)
MainFrame.BackgroundColor3 = Color3.fromRGB(12, 18, 12)
MainFrame.Active = true; MainFrame.Draggable = true
Instance.new("UICorner", MainFrame).CornerRadius = UDim.new(0, 8)
local MainStroke = Instance.new("UIStroke", MainFrame); MainStroke.Color = Color3.fromRGB(40, 160, 40); MainStroke.Thickness = 1.5

-- Botón Flotante "BEN" con RGB (Azul arriba, Verde medio, Rojo abajo)
local OpenBtn = Instance.new("TextButton", ScreenGui)
OpenBtn.Size = UDim2.new(0, 50, 0, 50); OpenBtn.Position = UDim2.new(0.02, 0, 0.4, 0)
OpenBtn.Text = "BEN"; OpenBtn.TextColor3 = Color3.new(1, 1, 1); OpenBtn.Font = Enum.Font.SourceSansBold
OpenBtn.BackgroundColor3 = Color3.new(1, 1, 1); OpenBtn.Visible = false; OpenBtn.Active = true; OpenBtn.Draggable = true
Instance.new("UICorner", OpenBtn).CornerRadius = UDim.new(1, 0)

local grad = Instance.new("UIGradient", OpenBtn)
grad.Rotation = 90
grad.Color = ColorSequence.new({
    ColorSequenceKeypoint.new(0, Color3.fromRGB(0, 0, 255)),   -- Azul arriba
    ColorSequenceKeypoint.new(0.5, Color3.fromRGB(0, 255, 0)), -- Verde medio
    ColorSequenceKeypoint.new(1, Color3.fromRGB(255, 0, 0))    -- Rojo abajo
})

local MinimizeBtn = Instance.new("TextButton", MainFrame)
MinimizeBtn.Size = UDim2.new(0, 28, 0, 28); MinimizeBtn.Position = UDim2.new(1, -68, 0, 8)
MinimizeBtn.Text = "-"; MinimizeBtn.BackgroundColor3 = Color3.fromRGB(30, 50, 30); MinimizeBtn.TextColor3 = Color3.new(1, 1, 1); MinimizeBtn.TextSize = 18
Instance.new("UICorner", MinimizeBtn)

local CloseBtn = Instance.new("TextButton", MainFrame)
CloseBtn.Size = UDim2.new(0, 28, 0, 28); CloseBtn.Position = UDim2.new(1, -34, 0, 8)
CloseBtn.Text = "X"; CloseBtn.BackgroundColor3 = Color3.fromRGB(150, 30, 30); CloseBtn.TextColor3 = Color3.new(1, 1, 1); CloseBtn.TextSize = 14
Instance.new("UICorner", CloseBtn)

MinimizeBtn.MouseButton1Click:Connect(function() MainFrame.Visible = false; OpenBtn.Visible = true end)
OpenBtn.MouseButton1Click:Connect(function() MainFrame.Visible = true; OpenBtn.Visible = false end)
CloseBtn.MouseButton1Click:Connect(function() ScreenGui:Destroy() end)

-- Menú Lateral Izquierdo
local Sidebar = Instance.new("ScrollingFrame", MainFrame)
Sidebar.Size = UDim2.new(0, 140, 1, -16); Sidebar.Position = UDim2.new(0, 8, 0, 8)
Sidebar.BackgroundColor3 = Color3.fromRGB(18, 25, 18); Sidebar.BorderSizePixel = 0; Sidebar.CanvasSize = UDim2.new(0, 0, 0, 0)
Instance.new("UICorner", Sidebar).CornerRadius = UDim.new(0, 6)
local SideLayout = Instance.new("UIListLayout", Sidebar); SideLayout.Padding = UDim.new(0, 6)

-- Contenedor Derecho
local Container = Instance.new("Frame", MainFrame)
Container.Size = UDim2.new(1, -160, 1, -50); Container.Position = UDim2.new(0, 155, 0, 42)
Container.BackgroundTransparency = 1

local Pages = {}
local function createPage()
    local page = Instance.new("ScrollingFrame", Container)
    page.Size = UDim2.new(1, 0, 1, 0); page.BackgroundTransparency = 1; page.Visible = false
    page.CanvasSize = UDim2.new(0, 0, 0, 500); page.ScrollBarThickness = 3
    Instance.new("UIListLayout", page).Padding = UDim.new(0, 8)
    return page
end

local function createTab(name)
    local btn = Instance.new("TextButton", Sidebar)
    btn.Size = UDim2.new(1, -8, 0, 36); btn.Position = UDim2.new(0, 4, 0, 0)
    btn.Text = "  " .. name; btn.TextColor3 = Color3.fromRGB(200, 230, 200); btn.TextXAlignment = Enum.TextXAlignment.Left
    btn.BackgroundColor3 = Color3.fromRGB(25, 40, 25); btn.Font = Enum.Font.SourceSansSemibold; btn.TextSize = 13
    Instance.new("UICorner", btn).CornerRadius = UDim.new(0, 5)
    
    local page = createPage()
    btn.MouseButton1Click:Connect(function()
        for _, p in pairs(Pages) do p.Visible = false end
        page.Visible = true
    end)
    table.insert(Pages, page)
    return page
end

local TabFarm = createTab("Farm & Loot")
local TabCombat = createTab("Combat & Skills")
local TabConfig = createTab("Config & Zenkai")

if Pages[1] then Pages[1].Visible = true end

local function addToggleToPage(page, title, flagName)
    local row = Instance.new("Frame", page)
    row.Size = UDim2.new(1, -10, 0, 36); row.BackgroundColor3 = Color3.fromRGB(18, 30, 18)
    Instance.new("UICorner", row).CornerRadius = UDim.new(0, 6)
    
    local lbl = Instance.new("TextLabel", row)
    lbl.Size = UDim2.new(0.68, 0, 1, 0); lbl.Position = UDim2.new(0, 10, 0, 0)
    lbl.Text = title; lbl.TextColor3 = Color3.fromRGB(220, 255, 220); lbl.BackgroundTransparency = 1
    lbl.TextXAlignment = Enum.TextXAlignment.Left; lbl.TextSize = 13
    
    local tBtn = Instance.new("TextButton", row)
    tBtn.Size = UDim2.new(0, 50, 0, 22); tBtn.Position = UDim2.new(1, -60, 0.5, -11)
    tBtn.Text = Flags[flagName] and "ON" or "OFF"; tBtn.TextSize = 11; tBtn.Font = Enum.Font.SourceSansBold
    tBtn.BackgroundColor3 = Flags[flagName] and Color3.fromRGB(0, 200, 80) or Color3.fromRGB(180, 50, 50)
    tBtn.TextColor3 = Color3.new(1, 1, 1)
    Instance.new("UICorner", tBtn).CornerRadius = UDim.new(1, 0)
    
    tBtn.MouseButton1Click:Connect(function()
        Flags[flagName] = not Flags[flagName]
        if flagName == "AutoZenkai" and Flags.AutoZenkai then ZenkaiCount = 0 end
        tBtn.Text = Flags[flagName] and "ON" or "OFF"
        tBtn.BackgroundColor3 = Flags[flagName] and Color3.fromRGB(0, 200, 80) or Color3.fromRGB(180, 50, 50)
        SaveSettings()
    end)
end

-- Rellenar pestañas
addToggleToPage(TabFarm, "Auto Farm Boss", "AutoFarmBoss")
addToggleToPage(TabFarm, "Auto Zeni (Zeno Zama)", "AutoZeni")
addToggleToPage(TabFarm, "Auto Grab Chests / Loot [F]", "AutoChest")
addToggleToPage(TabFarm, "Auto TP Dragon Balls", "AutoDragonBalls")

addToggleToPage(TabCombat, "Auto Lock Boss", "AutoLockBoss")
addToggleToPage(TabCombat, "Auto Click [E]", "AutoClick")
addToggleToPage(TabCombat, "Auto Skills (1-8)", "AutoSkills")
addToggleToPage(TabCombat, "Auto Especiales (Z,X,C,V,G)", "AutoCustomSkills")

addToggleToPage(TabConfig, "Auto Retry / Reintentar", "AutoRetry")
addToggleToPage(TabConfig, "Auto Zenkai", "AutoZenkai")

local ZenkaiBox = Instance.new("TextBox", TabConfig)
ZenkaiBox.Size = UDim2.new(1, -10, 0, 36); ZenkaiBox.BackgroundColor3 = Color3.fromRGB(18, 30, 18)
ZenkaiBox.PlaceholderText = "Cant. Zenkai"; ZenkaiBox.Text = tostring(Config.ZenkaiAmount)
ZenkaiBox.TextColor3 = Color3.new(1,1,1); ZenkaiBox.TextSize = 13
Instance.new("UICorner", ZenkaiBox)
ZenkaiBox.FocusLost:Connect(function() 
    Config.ZenkaiAmount = tonumber(ZenkaiBox.Text) or 10; ZenkaiCount = 0; SaveSettings()
end)

local SpeedBox = Instance.new("TextBox", TabConfig)
SpeedBox.Size = UDim2.new(1, -10, 0, 36); SpeedBox.BackgroundColor3 = Color3.fromRGB(18, 30, 18)
SpeedBox.PlaceholderText = "Delay Skill (s)"; SpeedBox.Text = tostring(Config.SkillDelay)
SpeedBox.TextColor3 = Color3.new(1,1,1); SpeedBox.TextSize = 13
Instance.new("UICorner", SpeedBox)
SpeedBox.FocusLost:Connect(function() 
    Config.SkillDelay = tonumber(SpeedBox.Text) or 1.5; SaveSettings()
end)
]]

local QueueOnTeleport = queue_on_teleport or (syn and syn.queue_on_teleport) or (fluxus and fluxus.queue_on_teleport) or nil
if QueueOnTeleport then QueueOnTeleport(AutoExecuteCode) end
loadstring(AutoExecuteCode)()
